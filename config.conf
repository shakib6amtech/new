# -------------------------------
# Auto Backup Configuration
# -------------------------------
PROJECT_PATH=/opt/lampp/htdocs/new
BACKUP_BRANCH=backup
TEMP_WORKTREE=/tmp/backup_worktree
INTERVAL=1          # in minutes
MAX_RETRY=3
RETRY_INTERVAL=10   # in seconds
LOG_DIR=/home/user/auto_backup/logs